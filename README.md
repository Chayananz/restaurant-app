# ระบบจัดการร้านอาหาร (Restaurant Management System)
## Desktop CLI Application - เชื่อมต่อ SQLite โดยตรง (ไม่ใช้ API)

##  สารบัญ
1. [ภาพรวมของระบบ](#ภาพรวมของระบบ)
2. [ทุกหน้ามันเชื่อมต่อกันยังไง](#ทุกหน้ามันเชื่อมต่อกันยังไง)
3. [เทคโนโลยีที่ใช้](#เทคโนโลยีที่ใช้)
4. [โครงสร้างไฟล์](#โครงสร้างไฟล์)
5. [ฐานข้อมูล (Database)](#ฐานข้อมูล-database)
6. [SQL Operations ทั้งหมด](#sql-operations-ทั้งหมด)
7. [การติดตั้งและรันโปรแกรม](#การติดตั้งและรันโปรแกรม)
8. [วิธีใช้งานโปรแกรม](#วิธีใช้งานโปรแกรม)

---

##  ภาพรวมของระบบ

ระบบจัดการร้านอาหารแบบ **Desktop CLI Application** ที่เชื่อมต่อ **SQLite Database โดยตรง** ผ่าน Node.js

###  จุดสำคัญของระบบ
- ✅ **ไม่มี API เลย** - ไม่มี Express, REST API, หรือ Web Server
- ✅ **เชื่อมต่อ Database โดยตรง** - ใช้ SQL Commands (INSERT, SELECT, UPDATE, DELETE) โดยตรง
- ✅ **Desktop Application** - ทำงานผ่าน Command Line Interface (CLI)
- ✅ **ไม่ต้องใช้ Web Browser** - ไม่มี Frontend HTML/CSS/JavaScript

### ความสามารถหลัก
- ✅ จัดการโต๊ะ (เพิ่ม แก้ไข ลบ เปลี่ยนสถานะ)
- ✅ จัดการเมนูอาหาร (เพิ่ม แก้ไข ลบ)
- ✅ จัดการพนักงาน (เพิ่ม แก้ไข ลบ)
- ✅ ระบบสั่งอาหาร (เลือกโต๊ะ เลือกพนักงาน เลือกเมนู)
- ✅ ดูประวัติออเดอร์ทั้งหมด
- ✅ สร้างและดูใบเสร็จ

---

##  เทคโนโลยีที่ใช้

### Backend
- **Node.js** - JavaScript Runtime
- **SQLite3** - ฐานข้อมูล (ไฟล์: [restaurant.db](restaurant.db))
- **Readline** - รับ Input จาก Command Line

### สิ่งที่ไม่ได้ใช้ (ถูกลบออกแล้ว)
- ~~Express.js~~ - ไม่ใช้ Web Framework
- ~~CORS~~ - ไม่มี Web Server
- ~~Body-Parser~~ - ไม่มี API
- ~~HTML/CSS/JavaScript Frontend~~ - ไม่มี Web Interface

---

##  โครงสร้างไฟล์

```
restaurant-app/
│
├── main.js                  # โปรแกรมหลัก CLI Application
├── restaurant.db            # SQLite database file
├── package.json             # Dependencies (เหลือแค่ sqlite3)
│
├── server.js                # (เก่า - ไม่ใช้แล้ว)
└── public/                  # (เก่า - ไม่ใช้แล้ว)
    ├── index.html
    ├── style.css
    └── script.js
```

---

##  ฐานข้อมูล (Database)

### ไฟล์: `restaurant.db` (SQLite Database)

### โครงสร้างตาราง

#### 1. **Table** (ตารางโต๊ะ)
```sql
CREATE TABLE Table (
    Table_ID INTEGER PRIMARY KEY AUTOINCREMENT,
    Table_Number INTEGER NOT NULL,
    Capacity INTEGER NOT NULL,
    Status TEXT NOT NULL DEFAULT 'ว่าง'
);
```

#### 2. **Employee** (ตารางพนักงาน)
```sql
CREATE TABLE Employee (
    Employee_ID INTEGER PRIMARY KEY AUTOINCREMENT,
    Employee_Name TEXT NOT NULL,
    Position TEXT NOT NULL
);
```

#### 3. **Menu** (ตารางเมนูอาหาร)
```sql
CREATE TABLE Menu (
    Menu_ID INTEGER PRIMARY KEY AUTOINCREMENT,
    Menu_Name TEXT NOT NULL,
    Menu_Price INTEGER NOT NULL,
    Category TEXT NOT NULL
);
```

#### 4. **Order** (ตารางออเดอร์)
```sql
CREATE TABLE Order (
    Order_ID INTEGER PRIMARY KEY AUTOINCREMENT,
    Order_Time TEXT NOT NULL,
    Total_Amount INTEGER NOT NULL,
    Order_Date TEXT NOT NULL,
    Employee_ID INTEGER,
    Table_ID INTEGER,
    FOREIGN KEY (Employee_ID) REFERENCES Employee(Employee_ID),
    FOREIGN KEY (Table_ID) REFERENCES Table(Table_ID)
);
```

#### 5. **OrderDetail** (ตารางรายละเอียดออเดอร์)
```sql
CREATE TABLE OrderDetail (
    OrderDetail_ID INTEGER PRIMARY KEY AUTOINCREMENT,
    Quantity INTEGER NOT NULL,
    Order_ID INTEGER,
    Menu_ID INTEGER,
    FOREIGN KEY (Order_ID) REFERENCES Order(Order_ID),
    FOREIGN KEY (Menu_ID) REFERENCES Menu(Menu_ID)
);
```

#### 6. **Receipt** (ตารางใบเสร็จ)
```sql
CREATE TABLE Receipt (
    Receipt_ID INTEGER PRIMARY KEY AUTOINCREMENT,
    Receipt_Time TEXT NOT NULL,
    Receipt_Date TEXT NOT NULL,
    Order_ID INTEGER,
    FOREIGN KEY (Order_ID) REFERENCES Order(Order_ID)
);
```

---

##  SQL Operations ทั้งหมด

### ไฟล์: [main.js](main.js)

---

##  # INSERT Operations (การเพิ่มข้อมูล)

**คำอธิบาย:** ใช้คำสั่ง `INSERT INTO` เพื่อเพิ่มข้อมูลใหม่ลงในฐานข้อมูล

| ลำดับ | บรรทัด | Function | ตาราง | SQL Operation | คำอธิบาย |
|-------|--------|----------|-------|---------------|----------|
| 1 | [117](main.js#L117) | `insertTable()` | `Table` | `INSERT INTO \`Table\` (Table_Number, Capacity, Status) VALUES (?, ?, ?)` | เพิ่มโต๊ะใหม่พร้อมหมายเลข ที่นั่ง และสถานะ |
| 2 | [196](main.js#L196) | `insertEmployee()` | `Employee` | `INSERT INTO Employee (Employee_Name, Position) VALUES (?, ?)` | เพิ่มพนักงานใหม่พร้อมชื่อและตำแหน่ง |
| 3 | [275](main.js#L275) | `insertMenu()` | `Menu` | `INSERT INTO Menu (Menu_Name, Menu_Price, Category) VALUES (?, ?, ?)` | เพิ่มเมนูอาหารใหม่พร้อมชื่อ ราคา และหมวดหมู่ |
| 4 | [379](main.js#L379) | `insertOrder()` | `Order` | `INSERT INTO \`Order\` (Order_Time, Total_Amount, Order_Date, Employee_ID, Table_ID) VALUES (?, ?, ?, ?, ?)` | สร้างออเดอร์ใหม่พร้อมเวลา ยอดรวม วันที่ พนักงาน และโต๊ะ |
| 5 | [390](main.js#L390) | `insertOrder()` | `OrderDetail` | `INSERT INTO OrderDetail (Quantity, Order_ID, Menu_ID) VALUES (?, ?, ?)` | เพิ่มรายละเอียดออเดอร์ (เมนูและจำนวน) ใช้ Prepared Statement |
| 6 | [474](main.js#L474) | `insertReceipt()` | `Receipt` | `INSERT INTO Receipt (Receipt_Time, Receipt_Date, Order_ID) VALUES (?, ?, ?)` | สร้างใบเสร็จพร้อมเวลา วันที่ และเชื่อมกับออเดอร์ |

**สรุป INSERT:** ใช้ทั้งหมด **6 ตำแหน่ง** ครอบคลุมทุกตารางที่ต้องการเพิ่มข้อมูล

---

##  # DELETE Operations (การลบข้อมูล)

**คำอธิบาย:** ใช้คำสั่ง `DELETE FROM` เพื่อลบข้อมูลออกจากฐานข้อมูล

| ลำดับ | บรรทัด | Function | ตาราง | SQL Operation | คำอธิบาย |
|-------|--------|----------|-------|---------------|----------|
| 1 | [154](main.js#L154) | `deleteTable()` | `Table` | `DELETE FROM \`Table\` WHERE Table_ID = ?` | ลบโต๊ะตาม ID |
| 2 | [233](main.js#L233) | `deleteEmployee()` | `Employee` | `DELETE FROM Employee WHERE Employee_ID = ?` | ลบพนักงานตาม ID |
| 3 | [312](main.js#L312) | `deleteMenu()` | `Menu` | `DELETE FROM Menu WHERE Menu_ID = ?` | ลบเมนูอาหารตาม ID |
| 4 | [408](main.js#L408) | `deleteOrder()` | `OrderDetail` | `DELETE FROM OrderDetail WHERE Order_ID = ?` | ลบรายละเอียดออเดอร์ทั้งหมดของออเดอร์นั้น |
| 5 | [415](main.js#L415) | `deleteOrder()` | `Receipt` | `DELETE FROM Receipt WHERE Order_ID = ?` | ลบใบเสร็จที่เชื่อมกับออเดอร์ |
| 6 | [423](main.js#L423) | `deleteOrder()` | `Order` | `DELETE FROM \`Order\` WHERE Order_ID = ?` | ลบออเดอร์ตาม ID (ลบหลังจากลบ OrderDetail และ Receipt แล้ว) |

**สรุป DELETE:** ใช้ทั้งหมด **6 ตำแหน่ง** โดยมีการลบแบบ Cascade สำหรับออเดอร์ (ลบ OrderDetail → Receipt → Order ตามลำดับ)

---

##  # UPDATE Operations (การแก้ไขข้อมูล)

**คำอธิบาย:** ใช้คำสั่ง `UPDATE` เพื่ออัพเดทข้อมูลที่มีอยู่ในฐานข้อมูล

| ลำดับ | บรรทัด | Function | ตาราง | SQL Operation | คำอธิบาย |
|-------|--------|----------|-------|---------------|----------|
| 1 | [127](main.js#L127) | `updateTable()` | `Table` | `UPDATE \`Table\` SET Table_Number = ?, Capacity = ?, Status = ? WHERE Table_ID = ?` | แก้ไขข้อมูลโต๊ะทั้งหมด (หมายเลข ที่นั่ง สถานะ) |
| 2 | [141](main.js#L141) | `updateTableStatus()` | `Table` | `UPDATE \`Table\` SET Status = ? WHERE Table_ID = ?` | อัพเดทสถานะโต๊ะ (ว่าง/กำลังใช้งาน/กำลังจัดการ) |
| 3 | [211](main.js#L211) | `updateEmployee()` | `Employee` | `UPDATE Employee SET Employee_Name = ?, Position = ? WHERE Employee_ID = ?` | แก้ไขข้อมูลพนักงาน (ชื่อและตำแหน่ง) |
| 4 | [290](main.js#L290) | `updateMenu()` | `Menu` | `UPDATE Menu SET Menu_Name = ?, Menu_Price = ?, Category = ? WHERE Menu_ID = ?` | แก้ไขข้อมูลเมนู (ชื่อ ราคา หมวดหมู่) |

**สรุป UPDATE:** ใช้ทั้งหมด **4 ตำแหน่ง** ครอบคลุมการแก้ไขข้อมูลหลัก (Table, Employee, Menu)

---

##  # SELECT Operations (การดึงข้อมูล)

**คำอธิบาย:** ใช้คำสั่ง `SELECT` เพื่อดึงข้อมูลจากฐานข้อมูล (รวมถึง JOIN หลายตาราง)

| ลำดับ | บรรทัด | Function | ตาราง | SQL Operation | คำอธิบาย |
|-------|--------|----------|-------|---------------|----------|
| 1 | [96](main.js#L96) | `getAllTables()` | `Table` | `SELECT * FROM \`Table\` ORDER BY Table_Number` | ดึงข้อมูลโต๊ะทั้งหมดเรียงตามหมายเลขโต๊ะ |
| 2 | [175](main.js#L175) | `getAllEmployees()` | `Employee` | `SELECT * FROM Employee` | ดึงข้อมูลพนักงานทั้งหมด |
| 3 | [254](main.js#L254) | `getAllMenu()` | `Menu` | `SELECT * FROM Menu ORDER BY Category, Menu_Name` | ดึงข้อมูลเมนูทั้งหมดเรียงตามหมวดหมู่และชื่อ |
| 4 | [323](main.js#L323) | `getAllOrders()` | `Order` + `Table` + `Employee` (JOIN) | `SELECT o.*, t.Table_Number, e.Employee_Name FROM \`Order\` o LEFT JOIN \`Table\` t ON o.Table_ID = t.Table_ID LEFT JOIN Employee e ON o.Employee_ID = e.Employee_ID ORDER BY o.Order_ID DESC` | ดึงออเดอร์ทั้งหมดพร้อมข้อมูลโต๊ะและพนักงาน เรียงจากใหม่ไปเก่า (JOIN 3 ตาราง) |
| 5 | [344](main.js#L344) | `getOrderDetails()` | `Order` + `Table` + `Employee` (JOIN) | `SELECT o.*, t.Table_Number, e.Employee_Name FROM \`Order\` o LEFT JOIN \`Table\` t ON o.Table_ID = t.Table_ID LEFT JOIN Employee e ON o.Employee_ID = e.Employee_ID WHERE o.Order_ID = ?` | ดึงข้อมูลออเดอร์ตาม ID พร้อมข้อมูลโต๊ะและพนักงาน (JOIN 3 ตาราง) |
| 6 | [349](main.js#L349) | `getOrderDetails()` | `OrderDetail` + `Menu` (JOIN) | `SELECT od.*, m.Menu_Name, m.Menu_Price FROM OrderDetail od LEFT JOIN Menu m ON od.Menu_ID = m.Menu_ID WHERE od.Order_ID = ?` | ดึงรายละเอียดออเดอร์พร้อมชื่อและราคาเมนู (JOIN 2 ตาราง) |
| 7 | [371](main.js#L371) | `insertOrder()` | `Menu` | `SELECT Menu_Price FROM Menu WHERE Menu_ID = ?` | ดึงราคาเมนูเพื่อคำนวณยอดรวมออเดอร์ |
| 8 | [441](main.js#L441) | `getAllReceipts()` | `Receipt` + `Order` (JOIN) | `SELECT r.*, o.Total_Amount, o.Order_Time, o.Order_Date FROM Receipt r LEFT JOIN \`Order\` o ON r.Order_ID = o.Order_ID ORDER BY r.Receipt_ID DESC` | ดึงใบเสร็จทั้งหมดพร้อมข้อมูลออเดอร์ (JOIN 2 ตาราง) |
| 9 | [488](main.js#L488) | `getReceiptDetails()` | `Receipt` + `Order` + `Table` + `Employee` (JOIN) | `SELECT r.*, o.Total_Amount, o.Order_Time, o.Order_Date, t.Table_Number, e.Employee_Name FROM Receipt r LEFT JOIN \`Order\` o ON r.Order_ID = o.Order_ID LEFT JOIN \`Table\` t ON o.Table_ID = t.Table_ID LEFT JOIN Employee e ON o.Employee_ID = e.Employee_ID WHERE r.Order_ID = ?` | ดึงข้อมูลใบเสร็จพร้อมข้อมูลออเดอร์ โต๊ะ และพนักงาน (JOIN 4 ตาราง) |
| 10 | [493](main.js#L493) | `getReceiptDetails()` | `OrderDetail` + `Menu` (JOIN) | `SELECT od.*, m.Menu_Name, m.Menu_Price FROM OrderDetail od LEFT JOIN Menu m ON od.Menu_ID = m.Menu_ID WHERE od.Order_ID = ?` | ดึงรายการอาหารในใบเสร็จพร้อมชื่อและราคา (JOIN 2 ตาราง) |

**สรุป SELECT:** ใช้ทั้งหมด **10 ตำแหน่ง** รวมถึง JOIN หลายตาราง เพื่อดึงข้อมูลที่สมบูรณ์

---

##  สรุปการใช้งาน SQL Operations ทั้งหมด

| Operation | จำนวนครั้งที่ใช้ | ตารางที่เกี่ยวข้อง | คำอธิบาย |
|-----------|-----------------|-------------------|----------|
| **INSERT** | 6 ครั้ง | Order, OrderDetail, Receipt, Table, Employee, Menu | ✅ ใช้เพิ่มข้อมูลใหม่ในทุกตารางหลัก |
| **DELETE** | 6 ครั้ง | Table, Employee, Menu, OrderDetail, Receipt, Order | ✅ ใช้ลบข้อมูลทุกตารางหลัก (รวม Cascade Delete) |
| **UPDATE** | 4 ครั้ง | Table (2 ครั้ง), Employee, Menu | ✅ ใช้แก้ไขข้อมูลหลัก ไม่มี UPDATE สำหรับ Order/Receipt เพราะเป็นข้อมูลที่ไม่ควรแก้ไข |
| **SELECT** | 10 ครั้ง | ทุกตาราง + JOIN หลายตาราง | ✅ ใช้ดึงข้อมูลครอบคลุมทุกความต้องการ รวม JOIN |

**รวมทั้งหมด:** **26 SQL Operations**

---

##  สรุปคำตอบคำถาม

### 1.  ระบบนี้ไม่มี API เลย
**ถูกต้อง:** ระบบนี้**ไม่มี API ทั้ง External API และ REST API** เชื่อมต่อ SQLite Database โดยตรงผ่าน SQL Commands

### 2. ✅ INSERT Operations
**คำตอบ:** โค้ดได้ใช้ **INSERT** ทั้งหมด **6 ตำแหน่ง**
- บรรทัด **117** - `insertTable()` (Table)
- บรรทัด **196** - `insertEmployee()` (Employee)
- บรรทัด **275** - `insertMenu()` (Menu)
- บรรทัด **379** - `insertOrder()` (Order)
- บรรทัด **390** - `insertOrder()` (OrderDetail)
- บรรทัด **474** - `insertReceipt()` (Receipt)

### 3. ✅ DELETE Operations
**คำตอบ:** โค้ดได้ใช้ **DELETE** ทั้งหมด **6 ตำแหน่ง**
- บรรทัด **154** - `deleteTable()` (Table)
- บรรทัด **233** - `deleteEmployee()` (Employee)
- บรรทัด **312** - `deleteMenu()` (Menu)
- บรรทัด **408** - `deleteOrder()` (OrderDetail)
- บรรทัด **415** - `deleteOrder()` (Receipt)
- บรรทัด **423** - `deleteOrder()` (Order)

### 4. ✅ UPDATE Operations
**คำตอบ:** โค้ดได้ใช้ **UPDATE** ทั้งหมด **4 ตำแหน่ง**
- บรรทัด **127** - `updateTable()` (แก้ไขข้อมูลโต๊ะ)
- บรรทัด **141** - `updateTableStatus()` (อัพเดทสถานะโต๊ะ)
- บรรทัด **211** - `updateEmployee()` (แก้ไขข้อมูลพนักงาน)
- บรรทัด **290** - `updateMenu()` (แก้ไขข้อมูลเมนู)

### 5. ✅ SELECT Operations
**คำตอบ:** โค้ดได้ใช้ **SELECT** ทั้งหมด **10 ตำแหน่ง**
- บรรทัด **96** - `getAllTables()` (ดึงโต๊ะทั้งหมด)
- บรรทัด **175** - `getAllEmployees()` (ดึงพนักงานทั้งหมด)
- บรรทัด **254** - `getAllMenu()` (ดึงเมนูทั้งหมด)
- บรรทัด **323** - `getAllOrders()` (ดึงออเดอร์ทั้งหมด + JOIN)
- บรรทัด **344** - `getOrderDetails()` (ดึงออเดอร์ตาม ID + JOIN)
- บรรทัด **349** - `getOrderDetails()` (ดึงรายละเอียดออเดอร์ + JOIN)
- บรรทัด **371** - `insertOrder()` (ดึงราคาเมนู)
- บรรทัด **441** - `getAllReceipts()` (ดึงใบเสร็จทั้งหมด + JOIN)
- บรรทัด **488** - `getReceiptDetails()` (ดึงใบเสร็จ + JOIN)
- บรรทัด **493** - `getReceiptDetails()` (ดึงรายการอาหารในใบเสร็จ + JOIN)

---

##  การติดตั้งและรันโปรแกรม

### 1. ติดตั้ง Dependencies

```bash
npm install
```

**Package ที่ใช้:**
- sqlite3 (เหลือแค่ตัวเดียว)

### 2. รันโปรแกรม

```bash
# รันโปรแกรม
npm start

# หรือ
node main.js

# Development mode (auto-reload)
npm run dev
```

### 3. ใช้งานผ่าน Command Line

โปรแกรมจะแสดงเมนูให้เลือก:
```
========================================
🍽️  ระบบจัดการร้านอาหาร
========================================
1. จัดการโต๊ะ (Tables)
2. จัดการพนักงาน (Employees)
3. จัดการเมนูอาหาร (Menu)
4. จัดการออเดอร์ (Orders)
5. จัดการใบเสร็จ (Receipts)
0. ออกจากโปรแกรม
========================================
```

---

##  วิธีใช้งานโปรแกรม

### 1. จัดการโต๊ะ (Tables)
```
1. ดูโต๊ะทั้งหมด (SELECT)
2. เพิ่มโต๊ะใหม่ (INSERT)
3. แก้ไขโต๊ะ (UPDATE)
4. ลบโต๊ะ (DELETE)
5. เปลี่ยนสถานะโต๊ะ (UPDATE)
```

**ตัวอย่างการใช้งาน:**
- เลือก `2` เพื่อเพิ่มโต๊ะใหม่
- กรอก `หมายเลขโต๊ะ: 5`
- กรอก `จำนวนที่นั่ง: 4`
- กรอก `สถานะ: ว่าง`

### 2. จัดการพนักงาน (Employees)
```
1. ดูพนักงานทั้งหมด (SELECT)
2. เพิ่มพนักงานใหม่ (INSERT)
3. แก้ไขพนักงาน (UPDATE)
4. ลบพนักงาน (DELETE)
```

### 3. จัดการเมนูอาหาร (Menu)
```
1. ดูเมนูทั้งหมด (SELECT)
2. เพิ่มเมนูใหม่ (INSERT)
3. แก้ไขเมนู (UPDATE)
4. ลบเมนู (DELETE)
```

**ตัวอย่างการใช้งาน:**
- เลือก `2` เพื่อเพิ่มเมนูใหม่
- กรอก `ชื่อเมนู: ข้าวผัด`
- กรอก `ราคา: 50`
- กรอก `หมวดหมู่: อาหารจานหลัก`

### 4. จัดการออเดอร์ (Orders)
```
1. ดูออเดอร์ทั้งหมด (SELECT)
2. สร้างออเดอร์ใหม่ (INSERT)
3. ดูรายละเอียดออเดอร์ (SELECT)
4. ลบออเดอร์ (DELETE)
```

**ตัวอย่างการสร้างออเดอร์:**
- เลือก `2` สร้างออเดอร์ใหม่
- กรอก `Table ID: 1`
- กรอก `Employee ID: 1`
- กรอก `จำนวนเมนูที่สั่ง: 2`
- กรอก `Menu ID รายการที่ 1: 3` และ `จำนวน: 2`
- กรอก `Menu ID รายการที่ 2: 5` และ `จำนวน: 1`

### 5. จัดการใบเสร็จ (Receipts)
```
1. ดูใบเสร็จทั้งหมด (SELECT)
2. สร้างใบเสร็จ (INSERT)
3. ดูรายละเอียดใบเสร็จ (SELECT)
```

**ตัวอย่างการสร้างใบเสร็จ:**
- เลือก `2` สร้างใบเสร็จ
- กรอก `Order ID: 1`

---

##  หมายเหตุสำคัญ

###  ฟีเจอร์ที่มีครบแล้ว
- ✅ **ไม่มี API เลย** - ไม่มี Express, REST API, หรือ Web Server
- ✅ **เชื่อมต่อ Database โดยตรง** - ใช้ SQL Commands โดยตรง
- ✅ INSERT Operations: 6 ตำแหน่ง
- ✅ DELETE Operations: 6 ตำแหน่ง
- ✅ UPDATE Operations: 4 ตำแหน่ง
- ✅ SELECT Operations: 10 ตำแหน่ง
- ✅ รวม SQL Operations: 26 ตำแหน่ง
- ✅ Desktop CLI Application

###  จุดเด่นของระบบ
1. **ไม่มี API** - ไม่มี Web Server, REST API, หรือ HTTP Request
2. **เชื่อมต่อ Database โดยตรง** - ใช้ SQLite3 module โดยตรง
3. **SQL Operations ครบถ้วน** - มีทั้ง INSERT, SELECT, UPDATE, DELETE
4. **JOIN หลายตาราง** - ดึงข้อมูลที่สมบูรณ์จากหลายตาราง
5. **Cascade Delete** - ลบข้อมูลที่เกี่ยวข้องอย่างถูกต้อง
6. **Interactive CLI** - ใช้งานง่าย มีเมนูชัดเจน
7. **Real-time Database Access** - ไม่ผ่าน API Endpoint

###  ข้อควรระวัง
- ต้องมี SQLite database ([restaurant.db](restaurant.db))
- ใช้งานผ่าน Command Line เท่านั้น
- ไม่มี Web Interface
- ข้อมูลจะหายถ้าลบไฟล์ .db

###  การเปลี่ยนแปลงจากเวอร์ชันเก่า
| เวอร์ชันเก่า (1.0.0) | เวอร์ชันใหม่ (2.0.0) |
|---------------------|---------------------|
| ✅ มี Express Server | ❌ ไม่มี Express |
| ✅ มี REST API (19 endpoints) | ❌ ไม่มี API เลย |
| ✅ มี Web Interface (HTML/CSS/JS) | ❌ ไม่มี Web Interface |
| ✅ ใช้ Port 3000 | ❌ ไม่ใช้ Port |
| ✅ เรียกผ่าน Browser | ❌ ใช้ผ่าน Command Line |
| ✅ Dependencies: express, cors, body-parser | ❌ Dependencies: เหลือแค่ sqlite3 |

---

**สร้างโดย:** Restaurant Management System Team
**เวอร์ชัน:** 2.0.0 (Desktop CLI Application - No API)
**อัพเดทล่าสุด:** 2025
**Architecture:** Direct SQLite Connection (No Express, No REST API, No Web Server)
